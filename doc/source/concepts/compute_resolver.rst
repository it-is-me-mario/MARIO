Compute Resolver
================

This page will explain the resolver-based compute architecture.

It will cover:

* the compute catalog;
* planner and resolver roles;
* parsed, extract, concat and formula strategies;
* runtime options such as ``compute_method`` and ``linear_strategy``.

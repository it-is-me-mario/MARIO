Tutorial 02: First SUT
======================

This tutorial will introduce a first SUT workflow in MARIO.

It will cover:

* loading a test SUT;
* split-native versus unified blocks;
* ``tech_assumption`` and the public matrix ``c``;
* switching assumptions with ``change_assumption(...)``.
